/**
 * Initialize app data - currently a no-op since real APIs are used
 * This file is kept for backwards compatibility with DataInitializer
 */

export function initializeAppData() {
	// No-op: App now uses real APIs for data fetching
	// Data is loaded on-demand by individual components
}
